service: aws-bardo-amigo-api

provider:
  name: aws
  runtime: nodejs18.x
  environment:
    OPENAI_KEY: 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

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: users/create
          method: get
  orchestrator:
    handler: app/Handlers/Orchestrator.orchestrator
    events:
      - http:
          path: orchestrator
          method: post

plugins:
  - serverless-offline
